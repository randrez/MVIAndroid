mutation quickInboundToWell(
    $taskId: ID!,
    $assetId: ID!,
    $conditionId: ID,
    $scannedAt: DateTime!,
    $toLocationId: ID!,
    $rackLocationId: ID,
    $facilityId: ID!,
    $tags: [String!],
    $userId: ID!,
    $userName: String!
) {
    quickInboundToWell(
        input: {
            task_id: $taskId,
            asset_id: $assetId,
            condition_id: $conditionId,
            scanned_at: $scannedAt,
            to_location_id: $toLocationId,
            facility_id: $facilityId,
            facility_location_id: $rackLocationId,
            tags: $tags,
            scanner_user_id: $userId,
            scanner_user_name: $userName
        }
    ) {
        id
        updated_at
    }
}